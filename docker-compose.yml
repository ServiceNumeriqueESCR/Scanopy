services:
  scanopy:
    image: scanopy/scanopy:latest
    container_name: scanopy
    restart: unless-stopped
    network_mode: host
    volumes:
      - scanopy_data:/app/data

volumes:
  scanopy_data:
